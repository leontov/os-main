Simple swarm simulation utilities for local testing.

run_swarm(nodes, steps, seed) -> dict

CLI example:

PYTHONPATH=. .venv/bin/python scripts/simulate_swarm.py --nodes 6 --steps 30 --seed 123 --json out.json --csv telemetry.csv
